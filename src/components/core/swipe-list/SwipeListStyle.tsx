import styled, { css } from 'styled-components';
import { FONT_SIZES } from 'constant/index';

export const SwipeListContainer = styled.div`
  // font-weight: ${({ isBold }) => (isBold ? 'bolder' : 'unset')};
`;
