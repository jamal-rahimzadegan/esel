import styled from 'styled-components';

export const InboxIcon = styled.div`
  clip-path: circle(50%);
  overflow: hidden;
  height: 30px;
  width: 30px;
  align-self: flex-end;

  img {
    object-fit: cover;
    transform: scale(1.4);
  }
`;
