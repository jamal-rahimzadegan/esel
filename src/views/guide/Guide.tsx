import React from 'react';
import { Card } from 'components';

interface GuideType {}

export default function Guide(props: GuideType) {
  return <Card className="d-flex flex-column align-items-center justify-content-center">صفحه راهنما</Card>;
}
